[project]
name = "support-ticket-env"
version = "1.0.0"
description = "OpenEnv environment for customer support ticket resolution"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.29.0",
    "pydantic>=2.6.0",
]